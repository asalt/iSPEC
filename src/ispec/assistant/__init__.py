"""Support assistant (LLM chatbot) utilities.

This package is intentionally isolated from the main iSPEC database so that
assistant chat logs can be stored separately (e.g. different retention policy,
privacy controls, and future training pipelines).
"""

